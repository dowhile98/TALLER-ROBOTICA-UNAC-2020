FUNC void save_array() {
int array_length;
int idx;
array_length = 320;

  exec("log > x.dat");
  for (idx = 0; idx < array_length; idx++) {
    printf ("%6.4f\n", singal_1K_5k_10k_15kHz[idx]);
  }
  exec("log off");
  
  exec("log > y.dat");
  for (idx = 0; idx < array_length; idx++) {
    printf ("%6.4f\n", y[idx]);
  }
  exec("log off");
  
   
}
